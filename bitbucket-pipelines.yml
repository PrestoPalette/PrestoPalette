image: cmake-qt5

pipelines:
  default:
    - step:
        script:
          - apt-get update -y
          - cmake --check-system-vars -G "Unix Makefiles" . -DCMAKE_BUILD_TYPE=Debug
          - make
          - cpack -G "TGZ"
          - cpack -G "RPM"
          - cpack -G "DEB"
          - cpack -G "NSIS64"

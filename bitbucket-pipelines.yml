image: gcc:6.1

pipelines:
  default:
    - step:
        script:
          - mkdir build && cd build
          - cmake ..
          - make
          - cpack -G "TGZ"
          - cpack -G "RPM"
          - cpack -G "DEB"
          - cpack -G "NSIS64"
